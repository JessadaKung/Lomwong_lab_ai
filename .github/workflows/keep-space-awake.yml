name: Keep Hugging Face Space Awake

on:
  schedule:
    - cron: "17 3 * * *"
  workflow_dispatch:

jobs:
  ping-space:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Space
        run: |
          curl --fail --location --max-time 30 \
            https://jetsadapa-lomwong-cafe.hf.space/
